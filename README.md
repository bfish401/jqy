# JQY - 前后端分离工具平台

一个基于 Python Flask 的前后端分离工具平台，支持 LDAP 和本地双认证方式。

---

## 🚀 快速开始

**🔥 准备开发？以下是你需要的 5 个文档：**

| 文档 | 用途 | 阅读时间 |
|-----|------|---------|
| **[📋 启动前检查清单](docs/LAUNCH_CHECKLIST.md)** | 开发前必读，确保环境、团队、工具都准备好 | 30 分钟 |
| **[📅 开发路线图](docs/DEVELOPMENT_ROADMAP.md)** | ALPHA 版本的详细计划，周度任务分解，工作量估算 | 20 分钟 |
| **[📊 产品准备度评估](docs/PRODUCT_READINESS.md)** | 产品角度的全面评估，是否可以开始开发 | 15 分钟 |
| **[🎨 UI/UX 设计规范](docs/UI_DESIGN.md)** | 像素级设计参数、配色方案、字体规范 | 15 分钟 |
| **[📡 API 接口规范](docs/API_DESIGN.md)** | 30+ 个端点的完整定义、示例、错误处理 | 20 分钟 |

---

## 📊 项目状态

```
✅ 产品定位和功能需求: 100% 完成
✅ UI/UX 设计规范: 100% 完成
✅ API 接口设计: 100% 完成
✅ 系统架构设计: 100% 完成
✅ 部署方案: 100% 完成
⚠️  代码实现: 准备启动
📍 当前阶段: 启动准备 (本周)
🎯 下阶段: ALPHA 版本开发 (4-6 周)
```

---

## 📋 项目概述

JQY 是一个功能灵活的企业级工具管理平台，灵感来自 JumpServer 的优秀架构设计。提供了：
- 🔐 双认证方式（LDAP + 本地认证）
- 💾 轻量级数据库存储（SQLite3，双库分离）
- 🎨 现代化 Web UI 界面
- 🚀 RESTful API 后端
- 📊 完整的权限管理和审计功能
- 🔌 可扩展的插件架构

**主要功能定位**：
- 企业工具资产管理
- 用户访问控制和权限管理
- 操作审计和日志记录
- 灵活的身份认证和授权

## ✨ 功能需求

### 用户认证模块

#### 登陆界面 (Login Page)

**功能需求**：
- 统一的登陆入口，支持多种认证方式
- 美观的登陆页面（响应式设计，适配各设备）
- 实时的认证方式切换

**认证方式**：
1. **LDAP 认证**
   - 企业 LDAP 目录服务集成
   - 自动用户创建和同步
   - 支持多个 LDAP 服务器配置

2. **本地认证**
   - 用户名/密码登陆
   - 支持用户自注册（可配置）
   - 记住密码功能

**登陆页面布局**：
```
┌─────────────────────────────────────────┐
│                                         │
│           JQY 工具管理平台              │
│        (Logo + 应用名称)                │
│                                         │
├─────────────────────────────────────────┤
│  📋 认证方式选择                        │
│  ┌─ LDAP 认证 ┐ ┌─ 本地认证 ┐         │
│  │            │ │          │         │
│  └────────────┘ └──────────┘         │
├─────────────────────────────────────────┤
│  用户名/邮箱: [ 输入框 ]               │
│  密码:       [ 输入框 ]               │
│  [ ☑ ] 记住密码                       │
│                                         │
│      [ 登陆 ] [ 忘记密码? ]            │
├─────────────────────────────────────────┤
│  第一次登陆? [立即注册]                 │
│  服务状态: ✓ 正常 | API: v1.0         │
└─────────────────────────────────────────┘
```

**安全特性**：
- 密码加密存储（bcrypt/argon2）
- 登陆失败次数限制（防暴力破解）
- 会话令牌管理（JWT）
- 登陆异常提示和审计

### 主界面和菜单布局 (Dashboard & Navigation)

**整体布局结构**：
```
┌────────────────────────────────────────────────────┐
│ JQY          🔔 消息(2) 👤 用户 ⚙️ 设置 🚪 登出     │  顶部导航栏
├──────┬──────────────────────────────────────────────┤
│      │                                              │
│  📌  │          主内容区域 (Main Content)            │
│  菜  │                                              │
│  单  │        · 仪表板（Dashboard）                  │
│  栏  │        · 资源管理                            │
│      │        · 用户管理                            │
│      │        · 权限管理                            │
│      │        · 审计日志                            │
│      │        · 系统设置                            │
│      │                                              │
│      │                                              │
│      │                                              │
└──────┴──────────────────────────────────────────────┘
```

**侧边栏菜单结构**：
```
┌─────────────────────┐
│ 📊 仪表板           │
├─────────────────────┤
│ 🏢 资产管理          │
│   ├─ 工具列表       │
│   ├─ 工具组         │
│   └─ 资产统计       │
├─────────────────────┤
│ 👥 用户和权限       │
│   ├─ 用户管理       │
│   ├─ 用户组         │
│   ├─ 角色管理       │
│   └─ 权限配置       │
├─────────────────────┤
│ 📝 审计和日志       │
│   ├─ 登陆日志       │
│   ├─ 操作审计       │
│   └─ 系统日志       │
├─────────────────────┤
│ ⚙️  系统设置        │
│   ├─ 基本配置       │
│   ├─ 认证设置       │
│   ├─ LDAP 配置     │
│   └─ 系统参数       │
├─────────────────────┤
│ ℹ️  关于和帮助      │
│   ├─ 关于           │
│   ├─ 文档           │
│   └─ 反馈           │
└─────────────────────┘
```

**顶部导航栏功能**：
- **左侧**：应用 Logo + 应用名称
- **中央**：当前页面面包屑导航
- **右侧**：
  - 🔔 消息/通知中心
  - 🔍 全局搜索
  - 👤 用户菜单（个人资料、设置、登出）
  - ⚙️ 系统设置（仅管理员可见）
  - 🎨 主题切换（浅/深色模式）

### 仪表板页面 (Dashboard Page)

**核心卡片和组件**：
```
┌─ 概览卡片 ─────────────────────────────────┐
│  系统概览                                  │
│  ┌──────────┐ ┌──────────┐ ┌──────────┐ │
│  │ 总工具数 │ │ 总用户数 │ │ 总组数   │ │
│  │   24     │ │   156    │ │   12     │ │
│  └──────────┘ └──────────┘ └──────────┘ │
│  ┌──────────┐ ┌──────────┐ ┌──────────┐ │
│  │ 在线用户 │ │ 今日操作 │ │ 系统状态 │ │
│  │   18     │ │   342    │ │  ✓ 正常 │ │
│  └──────────┘ └──────────┘ └──────────┘ │
└────────────────────────────────────────────┘

┌─ 最近活动 ─────────────────────────────────┐
│ 时间          │ 用户    │ 操作    │ 结果   │
│ 14:32:15     │ admin   │ 登陆    │ 成功   │
│ 14:28:42     │ user01  │ 访问    │ 成功   │
│ 14:25:18     │ user02  │ 修改    │ 失败   │
└────────────────────────────────────────────┘

┌─ 系统状态 ─────────────────────────────────┐
│ CPU 使用: [████████░░] 82%                 │
│ 内存使用: [██████░░░░] 68%                 │
│ 磁盘使用: [███░░░░░░░] 35%                 │
│ 数据库:   ✓ 连接正常                      │
└────────────────────────────────────────────┘
```

### 工具/资源管理 (Resource Management)

**工具列表页面**：
- 表格视图：工具名称、分类、所有者、状态、最后更新时间
- 卡片视图：缩略图展示
- 搜索和过滤：按名称、分类、状态检索
- 批量操作：选择、启用/禁用、删除

**工具详情页**：
- 基本信息：名称、描述、分类
- 访问权限：谁可以访问
- 连接方式：SSH、RDP、HTTP 等
- 审计日志：该工具的所有操作记录
- 关联用户：有权限的用户列表

### 用户和权限管理 (User & Permission Management)

**用户管理**：
```
用户列表表格：
┌─────────┬──────────┬────────┬──────────┬────────────┐
│ 用户名  │ 邮箱     │ 组织   │ 角色     │ 状态       │
├─────────┼──────────┼────────┼──────────┼────────────┤
│ admin   │ a@*.com  │ 系统   │ 管理员   │ 活跃 ✓     │
│ user01  │ u1@*.com │ 研发   │ 普通用户 │ 活跃 ✓     │
│ user02  │ u2@*.com │ 测试   │ 普通用户 │ 禁用 ✗     │
└─────────┴──────────┴────────┴──────────┴────────────┘

操作：编辑 | 重置密码 | 禁用/启用 | 删除 | 导出
```

**角色和权限管理**：
- 预定义角色：管理员、工具管理员、普通用户、访客
- 自定义角色：创建、编辑、删除
- 权限矩阵：细粒度权限控制（创建、读取、更新、删除）
- 权限继承：用户组继承权限

### 审计和日志 (Audit & Logging)

**登陆日志**：
- 记录所有用户登陆信息
- 显示：时间、用户、IP、浏览器、认证方式、结果
- 支持导出和搜索

**操作审计**：
- 记录用户的所有操作
- 显示：时间、用户、操作、对象、详情、结果
- 支持按操作类型过滤
- 合规性报表生成

**系统日志**：
- 应用级日志
- 数据库操作记录
- 错误和异常追踪

## 🛠 技术栈

### 后端
- **Python**: 3.9.7+
- **Web 框架**: Flask
- **数据库**: SQLite3（双库分离）
- **认证**: LDAP / 本地账户（JWT Token）
- **API**: RESTful API 架构
- **日志**: 结构化日志系统
- **缓存**: Redis（可选）

### 前端
- **框架**: Vue.js 3.x（或 React）
- **UI 组件库**: Element Plus / Ant Design
- **状态管理**: Vuex / Pinia
- **HTTP 客户端**: Axios
- **构建工具**: Vite / Webpack
- **响应式设计**: 移动端友好

### 基础设施
- **容器化**: Docker + Docker Compose
- **Web 服务器**: Gunicorn / uWSGI
- **反向代理**: Nginx
- **日志管理**: ELK Stack（可选）
- **监控告警**: Prometheus + Grafana（可选）

## 🏗 系统架构设计

### 微服务架构（可选）

```
┌──────────────────────────────────────────────────┐
│              用户浏览器 / 客户端                  │
└────────────┬─────────────────────────────────────┘
             │
┌────────────▼─────────────────────────────────────┐
│         API 网关 / Nginx 反向代理                 │
│         · 路由转发                               │
│         · 负载均衡                               │
│         · SSL/TLS 终止                           │
└────────────┬─────────────────────────────────────┘
             │
    ┌────────┴────────┐
    │                 │
┌───▼────────────┐  ┌─▼──────────────┐
│  前端服务      │  │  后端服务      │
│  (Lina)        │  │  (API Server)  │
│                │  │                │
│ · Vue.js       │  │ · Flask App    │
│ · Vite 构建    │  │ · 业务逻辑     │
│ · 静态资源     │  │ · 数据处理     │
└────────────────┘  └─┬──────────────┘
                      │
        ┌─────────────┼─────────────┐
        │             │             │
    ┌───▼──────┐  ┌──▼──────┐  ┌──▼──────┐
    │ 认证服务 │  │ 资产管理│  │ 审计日志│
    │ (Auth)   │  │(Resource)  │ (Audit) │
    │          │  │          │  │        │
    │ · LDAP   │  │ · CRUD   │  │ · 日志 │
    │ · JWT    │  │ · 权限   │  │ · 搜索 │
    │ · Session│  │ · 分类   │  │ · 报表 │
    └───┬──────┘  └──┬──────┘  └──┬─────┘
        │            │            │
        └────────────┼────────────┘
                     │
        ┌────────────┼────────────┐
        │            │            │
    ┌───▼──────┐ ┌──▼──────┐ ┌──▼──────┐
    │ System.db│ │Business │ │ Redis   │
    │ (系统库) │ │.db (业务)│ │ (缓存)  │
    └──────────┘ └─────────┘ └─────────┘
```

### 数据流设计

```
1. 用户登陆流程:
   用户输入凭证 → 前端验证 → API 提交
   → 后端认证（LDAP/本地）→ 生成 JWT
   → 返回 Token → 前端保存 Token
   → 后续请求携带 Token

2. 资源访问流程:
   用户请求资源 → 权限检查 → 数据库查询
   → 权限验证 → 返回数据
   → 审计日志记录

3. 审计日志流程:
   用户操作 → 事件拦截 → 日志序列化
   → 存储到审计库 → 索引优化
   → 支持查询和报表
```

## 📦 项目结构

```
jqy/
├── backend/                 # 后端代码
│   ├── app.py             # Flask 应用入口
│   ├── config.py          # 配置文件
│   ├── requirements.txt    # Python 依赖
│   ├── models/            # 数据模型
│   ├── routes/            # API 路由
│   ├── auth/              # 认证模块（LDAP + 本地）
│   └── database/          # 数据库初始化
├── frontend/              # 前端代码
│   ├── index.html
│   ├── css/
│   ├── js/
│   └── assets/
├── data/                  # 数据库文件目录
│   ├── system.db          # 系统配置数据库（只含表结构）
│   └── business.db        # 业务数据数据库（只含表结构）
├── README.md             # 项目说明
├── LICENSE               # 许可证
└── .gitignore
```

## 🚀 快速开始

### 前置要求

#### 本地部署
- Python 3.9.7 或更高版本
- pip 包管理器
- 现代浏览器

### 安装步骤

1. **克隆项目**
```bash
git clone https://github.com/bfish401/jqy.git
cd jqy
```

2. **创建 Python 虚拟环境**
```bash
python3.9 -m venv venv
source venv/bin/activate  # macOS/Linux
# 或
venv\Scripts\activate  # Windows
```

3. **安装依赖**
```bash
cd backend
pip install -r requirements.txt
```

4. **配置环境变量**
```bash
cp config.example.py config.py
# 编辑 config.py 配置运行环境和 LDAP 参数
```

5. **初始化数据库**
```bash
# 开发模式：包含示例数据
python database/init_db.py --mode development

# 或生产模式：仅包含表结构
python database/init_db.py --mode production
```

6. **启动后端服务**

**开发环境模式**（含开发数据）
```bash
python app.py --env dev
# 默认运行在 http://localhost:5000
# 包含示例用户、测试数据等
```

**生产环境模式**（仅表结构）
```bash
python app.py --env prod
# 默认运行在 http://0.0.0.0:5000
# 仅包含空表结构，无任何业务数据
```

7. **访问前端**
- 打开浏览器访问 `http://localhost:5000`

## ✨ 功能需求

### 用户认证模块

#### 登陆界面 (Login Page)

**功能需求**：
- 统一的登陆入口，支持多种认证方式
- 美观的登陆页面（响应式设计，适配各设备）
- 实时的认证方式切换

**认证方式**：
1. **LDAP 认证**
   - 企业 LDAP 目录服务集成
   - 自动用户创建和同步
   - 支持多个 LDAP 服务器配置

2. **本地认证**
   - 用户名/密码登陆
   - 支持用户自注册（可配置）
   - 记住密码功能

**登陆页面布局**：
```
┌─────────────────────────────────────────┐
│                                         │
│           JQY 工具管理平台              │
│        (Logo + 应用名称)                │
│                                         │
├─────────────────────────────────────────┤
│  📋 认证方式选择                        │
│  ┌─ LDAP 认证 ┐ ┌─ 本地认证 ┐         │
│  │            │ │          │         │
│  └────────────┘ └──────────┘         │
├─────────────────────────────────────────┤
│  用户名/邮箱: [ 输入框 ]               │
│  密码:       [ 输入框 ]               │
│  [ ☑ ] 记住密码                       │
│                                         │
│      [ 登陆 ] [ 忘记密码? ]            │
├─────────────────────────────────────────┤
│  第一次登陆? [立即注册]                 │
│  服务状态: ✓ 正常 | API: v1.0         │
└─────────────────────────────────────────┘
```

**安全特性**：
- 密码加密存储（bcrypt/argon2）
- 登陆失败次数限制（防暴力破解）
- 会话令牌管理（JWT）
- 登陆异常提示和审计

### 主界面和菜单布局 (Dashboard & Navigation)

**整体布局结构**：
```
┌────────────────────────────────────────────────────┐
│ JQY          🔔 消息(2) 👤 用户 ⚙️ 设置 🚪 登出     │  顶部导航栏
├──────┬──────────────────────────────────────────────┤
│      │                                              │
│  📌  │          主内容区域 (Main Content)            │
│  菜  │                                              │
│  单  │        · 仪表板（Dashboard）                  │
│  栏  │        · 资源管理                            │
│      │        · 用户管理                            │
│      │        · 权限管理                            │
│      │        · 审计日志                            │
│      │        · 系统设置                            │
│      │                                              │
│      │                                              │
│      │                                              │
└──────┴──────────────────────────────────────────────┘
```

**侧边栏菜单结构**：
```
┌─────────────────────┐
│ 📊 仪表板           │
├─────────────────────┤
│ 🏢 资产管理          │
│   ├─ 工具列表       │
│   ├─ 工具组         │
│   └─ 资产统计       │
├─────────────────────┤
│ 👥 用户和权限       │
│   ├─ 用户管理       │
│   ├─ 用户组         │
│   ├─ 角色管理       │
│   └─ 权限配置       │
├─────────────────────┤
│ 📝 审计和日志       │
│   ├─ 登陆日志       │
│   ├─ 操作审计       │
│   └─ 系统日志       │
├─────────────────────┤
│ ⚙️  系统设置        │
│   ├─ 基本配置       │
│   ├─ 认证设置       │
│   ├─ LDAP 配置     │
│   └─ 系统参数       │
├─────────────────────┤
│ ℹ️  关于和帮助      │
│   ├─ 关于           │
│   ├─ 文档           │
│   └─ 反馈           │
└─────────────────────┘
```

**顶部导航栏功能**：
- **左侧**：应用 Logo + 应用名称
- **中央**：当前页面面包屑导航
- **右侧**：
  - 🔔 消息/通知中心
  - 🔍 全局搜索
  - 👤 用户菜单（个人资料、设置、登出）
  - ⚙️ 系统设置（仅管理员可见）
  - 🎨 主题切换（浅/深色模式）

### 仪表板页面 (Dashboard Page)

**核心卡片和组件**：
```
┌─ 概览卡片 ─────────────────────────────────┐
│  系统概览                                  │
│  ┌──────────┐ ┌──────────┐ ┌──────────┐ │
│  │ 总工具数 │ │ 总用户数 │ │ 总组数   │ │
│  │   24     │ │   156    │ │   12     │ │
│  └──────────┘ └──────────┘ └──────────┘ │
│  ┌──────────┐ ┌──────────┐ ┌──────────┐ │
│  │ 在线用户 │ │ 今日操作 │ │ 系统状态 │ │
│  │   18     │ │   342    │ │  ✓ 正常 │ │
│  └──────────┘ └──────────┘ └──────────┘ │
└────────────────────────────────────────────┘

┌─ 最近活动 ─────────────────────────────────┐
│ 时间          │ 用户    │ 操作    │ 结果   │
│ 14:32:15     │ admin   │ 登陆    │ 成功   │
│ 14:28:42     │ user01  │ 访问    │ 成功   │
│ 14:25:18     │ user02  │ 修改    │ 失败   │
└────────────────────────────────────────────┘

┌─ 系统状态 ─────────────────────────────────┐
│ CPU 使用: [████████░░] 82%                 │
│ 内存使用: [██████░░░░] 68%                 │
│ 磁盘使用: [███░░░░░░░] 35%                 │
│ 数据库:   ✓ 连接正常                      │
└────────────────────────────────────────────┘
```

### 工具/资源管理 (Resource Management)

**工具列表页面**：
- 表格视图：工具名称、分类、所有者、状态、最后更新时间
- 卡片视图：缩略图展示
- 搜索和过滤：按名称、分类、状态检索
- 批量操作：选择、启用/禁用、删除

**工具详情页**：
- 基本信息：名称、描述、分类
- 访问权限：谁可以访问
- 连接方式：SSH、RDP、HTTP 等
- 审计日志：该工具的所有操作记录
- 关联用户：有权限的用户列表

### 用户和权限管理 (User & Permission Management)

**用户管理**：
```
用户列表表格：
┌─────────┬──────────┬────────┬──────────┬────────────┐
│ 用户名  │ 邮箱     │ 组织   │ 角色     │ 状态       │
├─────────┼──────────┼────────┼──────────┼────────────┤
│ admin   │ a@*.com  │ 系统   │ 管理员   │ 活跃 ✓     │
│ user01  │ u1@*.com │ 研发   │ 普通用户 │ 活跃 ✓     │
│ user02  │ u2@*.com │ 测试   │ 普通用户 │ 禁用 ✗     │
└─────────┴──────────┴────────┴──────────┴────────────┘

操作：编辑 | 重置密码 | 禁用/启用 | 删除 | 导出
```

**角色和权限管理**：
- 预定义角色：管理员、工具管理员、普通用户、访客
- 自定义角色：创建、编辑、删除
- 权限矩阵：细粒度权限控制（创建、读取、更新、删除）
- 权限继承：用户组继承权限

### 审计和日志 (Audit & Logging)

**登陆日志**：
- 记录所有用户登陆信息
- 显示：时间、用户、IP、浏览器、认证方式、结果
- 支持导出和搜索

**操作审计**：
- 记录用户的所有操作
- 显示：时间、用户、操作、对象、详情、结果
- 支持按操作类型过滤
- 合规性报表生成

**系统日志**：
- 应用级日志
- 数据库操作记录
- 错误和异常追踪

## 💾 数据库设计

### 双数据库架构

JQY 采用**双数据库分离设计**，有效解决了配置管理和业务数据隔离的问题：

#### 1. 系统配置数据库 (system.db)
- **用途**：存储系统部署相关的配置数据
- **包含内容**：
  - 用户账户信息（本地认证）
  - LDAP 配置
  - 系统参数设置
  - 权限角色配置
  - 日志记录
- **特点**：
  - 部署时**只保留表结构**，不包含数据
  - 可随程序共享，不涉及业务机密
  - 新部署环境自动初始化

#### 2. 业务数据数据库 (business.db)
- **用途**：存储所有业务相关数据
- **包含内容**：
  - 工具业务数据
  - 用户操作记录
  - 自定义配置数据
  - 其他业务相关内容
- **特点**：
  - 部署时**只保留表结构**，数据保留
  - 便于数据迁移和环境隔离
  - 支持数据备份和恢复

### 初始化方案

```python
# database/init_db.py
from database.models import init_system_db, init_business_db

def initialize_databases():
    """初始化两个数据库"""
    # 初始化系统配置数据库
    init_system_db('data/system.db')
    
    # 初始化业务数据数据库
    init_business_db('data/business.db')
```

### 数据库连接管理

```python
# backend/app.py
from database.connection import SystemDB, BusinessDB

system_db = SystemDB('data/system.db')      # 系统配置库
business_db = BusinessDB('data/business.db') # 业务数据库

# 在应用中使用
# system_db.query(...)  # 查询系统配置
# business_db.query(...) # 查询业务数据
```

### 部署指南

#### 开发环境
- 保留完整数据库文件（含表结构和数据）

#### 生产环境部署
1. **导出表结构**（仅保留 DDL）
```bash
sqlite3 data/system.db ".schema" > schemas/system_schema.sql
sqlite3 data/business.db ".schema" > schemas/business_schema.sql
```

2. **清空数据**（可选）
```bash
# 保留表结构，删除所有数据
sqlite3 data/business.db "DELETE FROM table_name;"
```

3. **共享程序**
- 共享时包含空表结构的 `.db` 文件
- 接收方初次运行自动创建表结构
- 现有数据不受影响

#### 新环境导入
```bash
# 导入表结构到新环境
sqlite3 data/system.db < schemas/system_schema.sql
sqlite3 data/business.db < schemas/business_schema.sql
```

### 开发和生产环境模式

#### 开发环境（Development Mode）

**特点**：
- 包含完整的示例和测试数据
- 便于快速开发和验证功能
- 调试模式启用，便于排查问题

**数据库初始化**：
```bash
python database/init_db.py --mode development
```

**启动方式**：
```bash
python app.py --env dev
```

**包含的示例数据**：
- 示例用户账户（本地认证）
- LDAP 连接配置示例
- 测试工具和业务数据
- 演示权限和角色配置

**配置特点**：
```python
# config.py for development
DEBUG = True
TESTING = True
LOG_LEVEL = 'DEBUG'
# LDAP 配置包含示例
LDAP_CONFIG = {...}
```

#### 生产环境（Production Mode）

**特点**：
- 仅包含空表结构，不含任何业务数据
- 适合部署到生产服务器
- 优化性能和安全性

**数据库初始化**：
```bash
python database/init_db.py --mode production
```

**启动方式**：
```bash
python app.py --env prod
```

**数据库内容**：
- 系统库：仅表结构，需手动创建系统配置
- 业务库：仅表结构，无任何业务数据

**配置特点**：
```python
# config.py for production
DEBUG = False
TESTING = False
LOG_LEVEL = 'INFO'
# 必须配置有效的 LDAP 或本地用户
LDAP_CONFIG = {...}
```

#### 共享和同步规则

**Git 版本控制**：
```bash
# .gitignore 配置
data/business.db     # 开发数据不提交
data/system.db       # 系统配置不提交（仅提交 schema）
*.pyc
__pycache__/
venv/
```

**推荐做法**：
1. 开发时使用 `--mode development` 本地开发
2. 提交代码前清理开发数据
3. 生产部署使用 `--mode production` 初始化
4. 使用 schema 文件版本控制表结构变更

**环境切换清单**：
```bash
# 从开发切换到生产
# 1. 备份开发数据库（可选）
cp data/business.db data/business.db.backup

# 2. 删除开发数据库
rm data/business.db data/system.db

# 3. 以生产模式初始化
python database/init_db.py --mode production

# 4. 以生产模式启动
python app.py --env prod
```

优势总结

| 方案 | 系统配置库 | 业务数据库 | 共享友好度 | 数据安全性 |
|-----|---------|---------|---------|---------|
| **双库设计** | ✅ 仅结构 | ✅ 结构+数据 | ⭐⭐⭐⭐⭐ | ⭐⭐⭐⭐⭐ |
| 单库方案 | ❌ 混合 | ❌ 混合 | ⭐⭐ | ⭐⭐⭐ |

## �🔐 认证配置

### LDAP 认证

在 `config.py` 中配置：
```python
LDAP_CONFIG = {
    'server': 'ldap://your-ldap-server.com',
    'port': 389,
    'base_dn': 'dc=example,dc=com',
    'user_dn_template': 'uid={username},ou=users,dc=example,dc=com'
}
```

### 本地认证

本地用户信息存储在 SQLite3 数据库中，支持：
- 用户注册
- 密码加密存储
- 用户信息管理

## 📚 API 文档

### 认证相关

#### 登录
```
POST /api/auth/login
Content-Type: application/json

{
    "username": "user",
    "password": "password",
    "auth_type": "ldap"  // 或 "local"
}

Response: 200 OK
{
    "token": "jwt_token",
    "user": {...}
}
```

#### 注册（本地认证）
```
POST /api/auth/register
Content-Type: application/json

{
    "username": "newuser",
    "password": "password",
    "email": "user@example.com"
}
```

#### 登出
```
POST /api/auth/logout
Authorization: Bearer {token}
```

## 📖 使用指南

1. **首次登录**：选择认证方式（LDAP 或本地）输入凭证

2. **用户管理**：在用户页面可以管理本地账户

3. **API 使用**：通过 REST API 与后端交互

## 🔧 配置说明

### 主要配置项

| 配置项 | 说明 | 开发环境 | 生产环境 |
|-------|------|--------|--------|
| `DEBUG` | 开发调试模式 | True | False |
| `ENVIRONMENT` | 运行环境 | development | production |
| `SECRET_KEY` | Flask 密钥 | 可简化 | 必须强密钥 |
| `SYSTEM_DB` | 系统配置数据库路径 | `./data/system.db` | `./data/system.db` |
| `BUSINESS_DB` | 业务数据数据库路径 | `./data/business.db` | `./data/business.db` |
| `LDAP_ENABLED` | 是否启用 LDAP | True/False | 需根据实际配置 |
| `JWT_EXPIRY` | Token 过期时间（秒） | 86400 | 3600 |
| `LOG_LEVEL` | 日志级别 | DEBUG | INFO |

### 数据库配置示例

```python
# config.py
import os

# 运行环境配置
ENVIRONMENT = os.getenv('APP_ENV', 'development')  # development 或 production
DEBUG = ENVIRONMENT == 'development'

# 数据库配置
DATABASE_DIR = os.path.join(os.path.dirname(__file__), '../data')
SYSTEM_DB = os.path.join(DATABASE_DIR, 'system.db')      # 系统配置库
BUSINESS_DB = os.path.join(DATABASE_DIR, 'business.db')   # 业务数据库

# 确保目录存在
os.makedirs(DATABASE_DIR, exist_ok=True)

# 环境特定配置
if ENVIRONMENT == 'development':
    # 开发环境配置
    TESTING = True
    LOG_LEVEL = 'DEBUG'
    JWT_EXPIRY = 86400  # 24 小时
    INIT_SAMPLE_DATA = True  # 初始化示例数据
else:
    # 生产环境配置
    TESTING = False
    LOG_LEVEL = 'INFO'
    JWT_EXPIRY = 3600   # 1 小时
    INIT_SAMPLE_DATA = False  # 不初始化示例数据
```

### 初始化脚本示例

```python
# database/init_db.py
import argparse
import sys
from models import Base
from engine import get_system_db, get_business_db

def init_databases(mode='development'):
    """
    初始化数据库
    
    Args:
        mode: 'development' 或 'production'
    """
    system_db = get_system_db()
    business_db = get_business_db()
    
    # 创建所有表
    Base.metadata.create_all(system_db.engine)
    Base.metadata.create_all(business_db.engine)
    
    if mode == 'development':
        # 初始化示例数据
        _init_sample_data(system_db, business_db)
        print("✓ 开发环境初始化完成（含示例数据）")
    else:
        print("✓ 生产环境初始化完成（仅表结构）")

def _init_sample_data(system_db, business_db):
    """初始化示例数据"""
    # 创建示例用户
    # 创建示例工具配置
    # 创建示例业务数据
    pass

if __name__ == '__main__':
    parser = argparse.ArgumentParser()
    parser.add_argument('--mode', choices=['development', 'production'], 
                       default='development', help='初始化模式')
    args = parser.parse_args()
    
    init_databases(args.mode)
```

### 应用启动脚本示例

```python
# backend/app.py
import os
import argparse
from flask import Flask
from config import ENVIRONMENT

def create_app():
    app = Flask(__name__)
    app.config.from_object('config')
    
    # 注册蓝图、初始化数据库等
    ...
    
    return app

if __name__ == '__main__':
    parser = argparse.ArgumentParser()
    parser.add_argument('--env', choices=['dev', 'prod'], 
                       default='dev', help='运行环境')
    args = parser.parse_args()
    
    # 设置环境变量
    os.environ['APP_ENV'] = 'development' if args.env == 'dev' else 'production'
    
    app = create_app()
    
    if args.env == 'dev':
        # 开发环境：启用热重载，127.0.0.1 访问
        app.run(debug=True, host='localhost', port=5000)
    else:
        # 生产环境：所有接口可访问，禁用调试
        app.run(debug=False, host='0.0.0.0', port=5000)
```

## 🤝 贡献指南

欢迎提交 Pull Request 或 Issue！

## � 功能对比

### JQY vs JumpServer

| 功能特性 | JQY | JumpServer | 说明 |
|--------|-----|-----------|------|
| LDAP 认证 | ✅ | ✅ | 企业目录服务集成 |
| 本地认证 | ✅ | ✅ | 用户名密码认证 |
| 权限管理 | ✅ | ✅ | RBAC 权限控制 |
| 审计日志 | ✅ | ✅ | 操作记录和追踪 |
| 仪表板 | ✅ | ✅ | 统计和概览 |
| SSH 支持 | ✅ | ✅ | 远程终端访问 |
| RDP 支持 | ⏳ | ✅ | 可视化远程桌面 |
| 数据库连接 | ⏳ | ✅ | 数据库管理界面 |
| Web 应用代理 | ⏳ | ✅ | 应用访问控制 |
| 资源群集 | ✅ | ✅ | 分布式资源管理 |
| 高可用部署 | ⏳ | ✅ | 多节点部署 |
| API 完整性 | ✅ | ✅ | RESTful API |
| 轻量级 | ✅ | ❌ | SQLite vs PostgreSQL |
| 单机部署 | ✅ | ⚠️ | 快速部署 |
| Docker 支持 | ✅ | ✅ | 容器化部署 |

**图例**: ✅ 已支持 | ⏳ 开发中 | ❌ 不支持 | ⚠️ 复杂

## 🗺 发展路线图 (Roadmap)

### V1.0 (当前)
- [x] 基础用户认证（LDAP + 本地）
- [x] 权限管理框架
- [x] 审计日志系统
- [x] 仪表板和基础 UI
- [x] API 框架
- [x] Docker 部署支持

### V1.1 (计划中)
- [ ] 用户界面优化
- [ ] 性能优化和缓存机制
- [ ] 更多认证方式（OAuth2、SAML）
- [ ] 高级搜索和过滤
- [ ] 数据导出功能
- [ ] 国际化支持（i18n）

### V1.2 (计划中)
- [ ] SSH Web 终端支持
- [ ] RDP 远程桌面支持（初版）
- [ ] 文件传输功能
- [ ] 命令审计和录制
- [ ] 会话管理

### V2.0 (长期规划)
- [ ] 微服务架构改造
- [ ] 高可用和分布式支持
- [ ] 数据库代理支持
- [ ] Web 应用代理
- [ ] 完整的 RDP 支持
- [ ] 移动端应用
- [ ] AI 辅助安全分析

## 🎯 项目对标

JQY 针对以下场景进行优化：
1. **轻量级部署** - 单机快速部署，无复杂依赖
2. **中小型企业** - 资源工具数量在 50-500 个范围
3. **快速开发** - 支持定制扩展，易于二次开发
4. **成本优化** - SQLite 数据库，降低硬件投入
5. **学习参考** - 完整的代码示例和文档

而 JumpServer 适合：
- 大型企业和复杂环境
- 需要高可用和灾备
- 资源规模庞大（1000+ 资产）
- 需要高级功能（数据库代理、应用网关等）

## 📈 性能指标

| 指标 | 目标值 | 说明 |
|-----|-------|------|
| 登陆响应时间 | < 500ms | 用户体验 |
| API 响应时间 | < 1000ms | 99% 请求 |
| 并发连接数 | 100+ | 单机部署 |
| 数据库容量 | 10GB+ | SQLite 支持 |
| 日志存储 | 可配置 | 自动轮转 |
| 可用性 | 99%+ | 生产环保 |

## �📝 许可证

本项目基于 [LICENSE](./LICENSE) 许可证发布

## 📧 联系方式

- 项目主页：[jqy.info](https://jqy.info)
- 提交 Issue：[GitHub Issues](https://github.com/bfish401/jqy/issues)

## 📖 更多文档

- [快速参考卡片](./docs/QUICK_REFERENCE.md) - 项目核心概念和常用命令速查
- [UI 设计规范](./docs/UI_DESIGN.md) - 登陆界面、菜单、组件设计规范
- [API 设计规范](./docs/API_DESIGN.md) - 完整的 API 接口设计和示例
- [Docker 部署指南](./DOCKER.md) - 详细的 Docker 使用说明
- [开发指南](./docs/DEVELOPMENT.md) - 开发相关的详细说明（待编写）

---

**最后更新**: 2025年11月22日
